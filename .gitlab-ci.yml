image: node
stages:
  - build
  - test
  - deploy
build site:
  stage: build
  script:
    - ls -ltra
    - pwd
    - whoami
    - npm --version
    - cd website
    - npm install
    - npm run build